# H2 内存数据库（开发用，可立即运行）
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:sicnugeo;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: 
  h2:
    console:
      enabled: true
      path: /h2-console
